import React from 'react'; import { cn } from '../../lib/utils'; export function PageHeader({eyebrow,title,description,actions,children,className}){return <header className={cn('nexus-page-header',className)}><div>{eyebrow&&<p className="nexus-eyebrow">{eyebrow}</p>}<h1>{title}</h1>{description&&<p>{description}</p>}</div>{(actions||children)&&<div className="nexus-page-actions">{actions||children}</div>}</header>}