import React from 'react'; import { motion,useReducedMotion } from 'framer-motion'; import { cn } from '../../lib/utils'; export function SurfaceCard({children,className,interactive=false,...props}){const reduced=useReducedMotion();return <motion.section className={cn('nexus-surface-card',interactive&&'nexus-surface-interactive',className)} whileHover={interactive&&!reduced?{y:-2}:undefined} transition={{type:'spring',stiffness:380,damping:30}} {...props}>{children}</motion.section>}