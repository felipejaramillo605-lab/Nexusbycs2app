import React from 'react';
import { AnimatePresence, motion, useReducedMotion } from 'framer-motion';
import { X } from 'lucide-react';
import { useAccessibleDialog } from './useAccessibleDialog';
// NEXUS_ACCESSIBILITY_4A_V1
export function DetailDrawer({ open, onClose, title, description, children }) { const reduced=useReducedMotion(); const {dialogRef,titleId,descriptionId}=useAccessibleDialog({open,onClose,titlePrefix:'nexus-detail-drawer'}); return <AnimatePresence>{open&&<><motion.button aria-label="Cerrar panel" className="nexus-drawer-overlay" onClick={onClose} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/><motion.aside ref={dialogRef} className="nexus-detail-drawer" role="dialog" aria-modal="true" aria-labelledby={titleId} aria-describedby={description?descriptionId:undefined} tabIndex={-1} initial={reduced?false:{x:'100%'}} animate={{x:0}} exit={reduced?undefined:{x:'100%'}} transition={{type:'spring',stiffness:380,damping:36}}><header><div><h2 id={titleId}>{title}</h2>{description&&<p id={descriptionId}>{description}</p>}</div><button type="button" onClick={onClose} className="nexus-icon-button" aria-label="Cerrar panel"><X size={18}/></button></header><div className="nexus-drawer-content">{children}</div></motion.aside></>}</AnimatePresence>; }
