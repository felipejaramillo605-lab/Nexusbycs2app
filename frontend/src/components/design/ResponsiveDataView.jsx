import React from 'react';
export function ResponsiveDataView({ columns, items, rowKey, renderCard, empty }) { if (!items?.length) return empty || null; return <><div className="nexus-data-desktop"><table className="nexus-data-table"><thead><tr>{columns.map(column => <th key={column.key} className={column.align === 'right' ? 'text-right' : ''}>{column.label}</th>)}</tr></thead><tbody>{items.map(item => <tr key={rowKey(item)}>{columns.map(column => <td key={column.key} className={column.align === 'right' ? 'text-right' : ''}>{column.render ? column.render(item) : item[column.key]}</td>)}</tr>)}</tbody></table></div><div className="nexus-data-mobile">{items.map(item => <article key={rowKey(item)} className="nexus-mobile-record">{renderCard(item)}</article>)}</div></>; }
