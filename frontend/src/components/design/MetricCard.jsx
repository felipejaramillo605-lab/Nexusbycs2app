import React from 'react'; import { motion,useReducedMotion } from 'framer-motion'; import { cn } from '../../lib/utils'; export function MetricCard({label,value,icon:Icon,detail,className,index=0}){const reduced=useReducedMotion();return <motion.article className={cn('nexus-metric-card',className)} initial={reduced?false:{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.28,delay:reduced?0:Math.min(index*.045,.22)}}>{Icon&&<span className="nexus-metric-icon"><Icon size={19}/></span>}<strong>{value}</strong><span>{label}</span>{detail&&<small>{detail}</small>}</motion.article>}