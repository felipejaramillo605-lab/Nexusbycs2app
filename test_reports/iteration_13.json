{
  "summary": "Smoke tested Auth Expansion (Email/Google tabs + Register) and Clients CRM route guard. Backend endpoints for register/login/clients respond correctly. Frontend Login/Register/route-guard flows work. Deep authenticated flows (ManagerClients table, history slide-over, WhatsApp modal) NOT testable without approved manual account or Google session; UI-level render verified only via unauth redirect.",
  "tested_scope": ["frontend", "backend", "auth", "integration"],
  "passed": [
    "POST /api/auth/register -> 200 with {message: 'Registration successful. Awaiting admin approval.', user_id}",
    "POST /api/auth/login with invalid creds -> 401 {detail: 'Invalid credentials'}",
    "GET /api/clients unauthenticated -> 401 {detail: 'Not authenticated'} (RLS guard OK)",
    "/login page loads with Email + Google tabs and 'Crear cuenta' link",
    "Email login form fills and submits; UI shows 'Invalid credentials' toast on bad creds",
    "/register page loads with password rules (Mínimo 8, mayúscula, número) visible",
    "/manager/clients unauth -> correctly redirects to /login (route guard works)"
  ],
  "failed": [],
  "not_tested": [
    "Successful manual login end-to-end -> dashboard (no pre-approved test user available)",
    "Register -> pending-approval page redirect (would create real DB record; skipped for T1 smoke)",
    "ManagerClients table population, 'Ver Historial' slide-over, WhatsApp mock modal (require authenticated manager session)",
    "GET /api/clients/{id}/history with valid auth",
    "Clientes nav button on ManagerDashboard (desktop + mobile)"
  ],
  "action_items": [
    "For full testing pass: provide/create an approved manual test user (e.g., test@nexus.com/Test123 pre-approved) so authenticated CRM/WhatsApp flows can be exercised end-to-end.",
    "Consider seeding a sample client + appointment for the test org so ManagerClients table + history modal have data to render."
  ],
  "success_rate": {"backend": "100% (3/3 smoke)", "frontend": "100% (4/4 smoke)"},
  "retest_needed": false,
  "full_testing_deferred": true,
  "mocked_apis_confirmed": ["WhatsApp Business API (whatsappService.js) - not exercised in this smoke run"]
}
